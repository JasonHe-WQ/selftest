{
  "version": "0,2,0",
  "configurations": [
    {
      "name": "cuda-test",
      "type": "cuda-gdb",
      "request": "launch",
      "program": "${workspaceFolder}/build/bin/histogram"
    }
  ]
}